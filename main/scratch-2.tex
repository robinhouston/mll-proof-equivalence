\documentclass{beamer}

\usepackage{willemtools,mllequiv}
\let\alt=\origalt
\usepackage{tikz}
\usepackage{pgffor}


%\makeTheoremDefs


\newcommand\octoroll[1]{
	\begin{tikzpicture}[x=5mm,y=-3mm,line width=.8pt,>=latex]
		\foreach \i/\n in {-5/a,-3/1,-1/b,1/2,3/3,5/4,7/c,9/5,11/6,13/7,15/8,17/9} {\node[bullet] (u\n) at (1,\i) {};}
		\foreach \i/\n in {-5/a,-3/1,-1/2,1/3,3/4,5/b,7/5,9/6,11/7,13/c,15/8,17/9} {\node[bullet] (w\n) at (17,\i) {};}
		\draw (ua)--(u1)  (ub)--(u2)--(u3)--(u4)  (uc)--(u5)--(u6)--(u7)--(u8)--(u9);
		\draw (wa)--(w1)--(w2)--(w3)--(w4)  (wb)--(w5)--(w6)--(w7)  (wc)--(w8)--(w9);
		\draw[rounded corners] (0,-6) rectangle (2,18);
		\draw[rounded corners] (16,-6) rectangle (18,18);
		\node[bullet] (u) at  (1,19) {}; \draw (u)-- (1,18);
		\node[bullet] (w) at (17,19) {}; \draw (w)--(17,18);
		\foreach \i/\n in {6/a,7/1,8/2,9/3,10/4,12/c,13/5,14/6,15/7,16/8,17/9}  {\node[circ] (v\n) at (9,\i) {};}
		\foreach \i/\n in {-5/a,-4/1,-3/2,-2/3,-1/4,0/5,1/6,2/7,3/8,4/9} {\node[circ] (a\n) at (9,\i) {};}
		\draw[rounded corners] (8,5.3) rectangle (10,10.7);
		\draw[rounded corners] (8,11.3) rectangle (10,17.7);
		\draw (9,10.7)--(9,11.3);
		\node[circ] (v) at (9,19) {};
		\draw[rounded corners] (7.5,5) rectangle (10.5,20);
		%%
	\begin{scope}[thepink,->]
	\SWITCH{#1}
	{1}{
			\draw (u)--(v); \draw (w)--(v);
			\draw (ub)--(va); \draw (uc)--(vc); \draw (wb)--(aa); \draw (wc)--(aa);
			\foreach \i in {1,...,9,a} {\draw (u\i)--(v\i); \draw (w\i)--(a\i);}
		}
	{2}{
			\draw (u)--(aa); \draw (w)--(v);
			\draw (ub)--(va); \draw (uc)--(vc); \draw (wb)--(aa); \draw (wc)--(aa);
			\foreach \i in {1,...,9,a} {\draw (u\i)--(v\i); \draw (w\i)--(a\i);}
		}
	{3}{
			\draw (u)--(aa); \draw (w)--(v);
			\draw (ub)--(va); \draw (wb)--(aa); \draw (wc)--(aa);
			\foreach \i in {1,...,9,a,c} {\draw (u\i)--(v\i);}
			\foreach \i in {1,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {a} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {} {\draw (v\i)--(a\j);}
		}
	{4}{
			\draw (u)--(aa); \draw (w)--(v);
			\draw (ub)--(va); \draw (wb)--(vc); \draw (wc)--(aa);
			\foreach \i in {1,...,9,a,c} {\draw (u\i)--(v\i);}
			\foreach \i in {1,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {a} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {} {\draw (v\i)--(a\j);}
		}
	{5}{
			\draw (u)--(aa); \draw (w)--(v);
			\draw (ub)--(va); \draw (wb)--(vc);
			\foreach \i in {1,...,9,a,c} {\draw (u\i)--(v\i);}
			\foreach \i in {1,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {} {\draw (v\i)--(a\j);}
		}
	{6}{
			\draw (u)--(aa); \draw (w)--(v);
			\draw (ub)--(va); \draw (wb)--(vc);
			\foreach \i in {1,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {1,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1} {\draw (u\i)--(a\j);}
		}
	{7}{
			\draw (u)--(aa); \draw (w)--(v);
			\draw (ub)--(va); \draw (wb)--(vc);
			\foreach \i in {1,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {2,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1} {\draw (u\i)--(a\j);}
		}
	{8}{
			\draw (u)--(aa); \draw (w)--(v);
			\draw (ub)--(va); \draw (wb)--(vc);
			\foreach \i in {2,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {2,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2} {\draw (u\i)--(a\j);}
		}
	{9}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {2,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {2,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2} {\draw (u\i)--(a\j);}
		}
	{10}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {2,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {3,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,2,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2} {\draw (u\i)--(a\j);}
		}
	{11}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {3,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {3,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,2,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3} {\draw (u\i)--(a\j);}
		}
	{12}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {3,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {4,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,3,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3} {\draw (u\i)--(a\j);}
		}
	{13}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {4,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {4,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,3,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4} {\draw (u\i)--(a\j);}
		}
	{14}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {4,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {5,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,4,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4} {\draw (u\i)--(a\j);}
		}
	{15}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {5,...,9,c} {\draw (u\i)--(v\i);}
			\foreach \i in {5,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,4,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5} {\draw (u\i)--(a\j);}
		}
	{16}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {5,...,9} {\draw (u\i)--(v\i);}
			\foreach \i in {5,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,4,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5} {\draw (u\i)--(a\j);}
		}
	{17}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {5,...,9} {\draw (u\i)--(v\i);}
			\foreach \i in {6,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,5,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5} {\draw (u\i)--(a\j);}
		}
	{18}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {6,...,9} {\draw (u\i)--(v\i);}
			\foreach \i in {6,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,5,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6} {\draw (u\i)--(a\j);}
		}
	{19}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {6,...,9} {\draw (u\i)--(v\i);}
			\foreach \i in {7,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,6,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6} {\draw (u\i)--(a\j);}
		}
	{20}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {7,...,9} {\draw (u\i)--(v\i);}
			\foreach \i in {7,...,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,6,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7} {\draw (u\i)--(a\j);}
		}
	{21}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {7,...,9} {\draw (u\i)--(v\i);}
			\foreach \i in {8,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,7,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7} {\draw (u\i)--(a\j);}
		}
	{22}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {8,9} {\draw (u\i)--(v\i);}
			\foreach \i in {8,9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,7,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8} {\draw (u\i)--(a\j);}
		}
	{23}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc); \draw (w8)--(v9);
			\foreach \i in {8,9} {\draw (u\i)--(v\i);}
			\foreach \i in {9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,7,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8} {\draw (u\i)--(a\j);}
		}
	{24}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc); \draw (w8)--(v9);
			\foreach \i in {8} {\draw (u\i)--(v\i);}
			\foreach \i in {9} {\draw (w\i)--(a\i);}
			\foreach \i in {1,...,7,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8,9/9} {\draw (u\i)--(a\j);}
		}
	{25}{
			\draw (u)--(aa); \draw (w)--(v); \draw (wb)--(vc); \draw (w8)--(v9); \draw (w9)--(aa);
			\foreach \i in {8} {\draw (u\i)--(v\i);}
			\foreach \i in {1,...,7,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8,9/9} {\draw (u\i)--(a\j);}
		}
	{26}{
			\draw (u)--(v9); \draw (w)--(v); \draw (wb)--(vc); \draw (w8)--(v9); \draw (w9)--(aa);
			\foreach \i in {8} {\draw (u\i)--(v\i);}
			\foreach \i in {1,...,7,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8,9/9} {\draw (u\i)--(a\j);}
		}
	{27}{
			\draw (u)--(v9); \draw (w)--(v); \draw (wb)--(vc); \draw (w9)--(aa);
			\foreach \i in {8} {\draw (u\i)--(v\i);}
			\foreach \i in {1,...,8,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8,9/9} {\draw (u\i)--(a\j);}
		}
	{28}{
			\draw (u)--(v9); \draw (w)--(v); \draw (wb)--(vc); \draw (w9)--(aa);
			\foreach \i in {1,...,8,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8,8/a,9/9} {\draw (u\i)--(a\j);}
		}
	{29}{
			\draw (u)--(v9); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {1,...,9,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8,8/a,9/9} {\draw (u\i)--(a\j);}
		}
	{30}{
			\draw (u)--(v); \draw (w)--(v); \draw (wb)--(vc);
			\foreach \i in {1,...,9,a,c} {\draw (w\i)--(v\i);}
			\foreach \i/\j in {a/1,1/2,b/2,2/3,3/4,4/5,c/5,5/6,6/7,7/8,8/a,9/9} {\draw (u\i)--(a\j);}
		}
	\DEFAULT{}
	\end{scope}
\end{tikzpicture}
}


\begin{document}

\foreach \n in {1,...,30} {%
  \begin{frame}
    \[
		\octoroll {\n}
	\]
  \end{frame}%
}


\end{document}
